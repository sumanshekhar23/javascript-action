name: "Hello World!"
description: "Greet someone and record the time"
inputs:
  who-to-greet:
    description: "whom to greet"
    required: true
    default: "World!"
outputs:
  time:
    description: "Time of greeting"
runs:
  using: "node20"
  main: "index.js"
